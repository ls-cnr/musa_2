/* delay before starting a dummy request */
delay(dummy_monitor,2000).

/* delta between two configuration generation from graph inspection */
delay(search_configuration,1000).

/* delta between two configuration lookup */
delay(wait_configuration,500).
