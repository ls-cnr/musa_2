package pmr.graph;

import layer.awareness.AbstractCapability;

public class NormalEdge extends Edge {

	public NormalEdge(Node source, Node destination, AbstractCapability capability){
		super(source, destination, capability);
	}
}
